<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>cern.c2mon.server</groupId>
    <artifactId>distribution</artifactId>
    <version>1.8.41-SNAPSHOT</version>
  </parent>

  <artifactId>docker</artifactId>
  <name>c2mon-server :: docker image</name>
  <packaging>ear</packaging>

  <properties>
    <registry>gitlab-registry.cern.ch</registry>
    <docker.image.base>openjdk:8-jdk</docker.image.base>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>2.10</version>
        <executions>
          <execution>
            <id>unpack</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>cern.c2mon.server</groupId>
                  <artifactId>c2mon-server</artifactId>
                  <type>tar.gz</type>
                </artifactItem>
              </artifactItems>
              <overWriteReleases>false</overWriteReleases>
              <overWriteSnapshots>true</overWriteSnapshots>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>1.0.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <id>dockerBuild</id>
            <goals>
              <goal>dockerBuild</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <from>
            <image>${docker.image.base}</image>
          </from>
          <to>
            <image>alkoclick/c2mon</image>
            <tags>
              <tag>${project.version}</tag>
              <tag>latest</tag>
            </tags>
          </to>
          <container>
            <useCurrentTimestamp>true</useCurrentTimestamp>
            <ports>
              <port>61616</port>
              <port>9200</port>
              <port>9001</port>
              <port>1099</port>
            </ports>
            <environment>
              <_JAVA_OPTIONS>-Dc2mon.server.elasticsearch.home=/c2mon-server/elasticsearch</_JAVA_OPTIONS>
            </environment>
            <volumes>
              <volume>/log</volume>
              <volume>/conf</volume>
              <volume>/elasticsearch</volume>
            </volumes>
            <entrypoint>["bin/c2mon.sh","run"]</entrypoint>
          </container>
          <extraDirectory>
            <path>${project.build.directory}/dependency/c2mon-server-${project.version}</path>
            <permissions>
              <permission>
                <file>/bin/c2mon.sh</file>
                <mode>755</mode>
              </permission>
              <permission>
                <file>/bin/C2MON-SRV.jvm</file>
                <mode>755</mode>
              </permission>
            </permissions>
          </extraDirectory>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-install-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
