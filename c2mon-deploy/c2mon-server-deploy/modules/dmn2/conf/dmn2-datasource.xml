<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
  xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
	                    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
	                    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
                       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

  <!-- must contain a bean with id cacheDataSource of type java.sql.DataSource -->
  <!-- must contain a bean with id configurationDataSource of type java.sql.DataSource -->
  
  <!-- datasource for the cache persistence -->
  <bean id="cacheDataSource" class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
    <property name="url" value="${jdbc.timpro.url}"/>
    <property name="username" value="${jdbc.timpro.user}"/>
    <property name="password" value="${jdbc.timpro.password}"/>
    <property name="defaultAutoCommit" value="false"/>
    <property name="validationQuery" value="SELECT 0 FROM DUAL"/>
<!--    <property name="logWriter" value=""/>-->
    <property name="initialSize" value="${datasource.persistence.connections.initial}"/>
    <property name="maxActive" value="${datasource.persistence.connections.max}"/>
  </bean>
  
  <!-- data source for loading configurations -->
  <bean id="configurationDataSource" class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
    <property name="url" value="${jdbc.config.url}"/>
    <property name="username" value="${jdbc.config.user}"/>
    <property name="password" value="${jdbc.config.password}"/>
<!--    <property name="defaultAutoCommit" value="false"/>-->
<!--    <property name="logWriter" value=""/>-->
    <property name="testWhileIdle" value="true"/>
    <property name="timeBetweenEvictionRunsMillis" value="600000"/>
    <property name="numTestsPerEvictionRun" value="${datasource.config.connections.initial}"/>
    <property name="validationQuery" value="SELECT 0 FROM DUAL"/>
    <property name="initialSize" value="${datasource.config.connections.initial}"/>
    <property name="maxActive" value="${datasource.config.connections.max}"/>
  </bean>
  
</beans>
