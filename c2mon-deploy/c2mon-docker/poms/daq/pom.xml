
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>cern.c2mon.docker</groupId>
    <artifactId>daq-parent</artifactId>
    <packaging>pom</packaging>
    <version>1</version>
    <name>C2MON Parent POM for DAQ Docker Images</name>
    <description>C2MON Parent POM for DAQ Docker Images</description>

    <dependencies>
        <dependency>
            <groupId>cern.c2mon.c2mon-daq</groupId>
            <artifactId>c2mon-daq-${c2mon.daqname}</artifactId>
            <version>${project.version}</version>
            <classifier>dist</classifier>
            <type>tar.gz</type>
        </dependency>
    </dependencies>
    <build>
      <resources>
        <resource>
            <directory>${project.basedir}/../../poms/daq/src/main/resources</directory>
            <filtering>true</filtering>
        </resource>
        <resource>
            <directory>src/main/resources</directory>
            <filtering>true</filtering>
        </resource>
      </resources>
    
      <pluginManagement>
        <plugins>
        
          <plugin>
             <groupId>org.apache.maven.plugins</groupId>
             <artifactId>maven-dependency-plugin</artifactId>
             <version>2.10</version>
             <executions>
               <execution>
                 <id>unpack</id>
                 <phase>generate-sources</phase>
                 <goals>
                   <goal>unpack</goal>
                 </goals>
                 <configuration>
                   <artifactItems>
                     <artifactItem>
                        <groupId>cern.c2mon.c2mon-daq</groupId>
                        <artifactId>c2mon-daq-${c2mon.daqname}</artifactId>
                        <version>${project.version}</version>
                        <classifier>dist</classifier>
                        <type>tar.gz</type>
                     </artifactItem>
                   </artifactItems>
                   <outputDirectory>${project.build.directory}/c2mon-${project.version}</outputDirectory>
                   <overWriteReleases>false</overWriteReleases>
                   <overWriteSnapshots>true</overWriteSnapshots>
                 </configuration>
               </execution>
             </executions>
           </plugin>        
        
          <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>0.4.5</version>
                <executions>
                     <execution>
                       <id>build-image</id>
                       <phase>compile</phase>
                       <goals> <goal>build</goal> </goals>
                     </execution>
                </executions>
                <configuration>
                    <imageName>docker.cern.ch/c2mon-project/${project.artifactId}</imageName>
                    <baseImage>${base.image}</baseImage>
                    
                    <useConfigFile>true</useConfigFile>
                    <forceTags>true</forceTags>
                    <imageTags>
                       <imageTag>${project.version}</imageTag>
                       <!--imageTag>latest</imageTag-->
                    </imageTags>
                    <workdir>/opt/c2mon/${project.artifactId}</workdir>
                    <entryPoint>["/docker-entrypoint.sh"]</entryPoint>
                    <exposes>
                      <expose>1099</expose>
                    </exposes>
                    <runs>
                      <run>chmod 755 /opt/c2mon/${project.artifactId}/bin/*</run>
                      <run>mkdir /opt/c2mon/${project.artifactId}/tmp</run>
                      <run>apt-get install -y --no-install-recommends xz-utils</run> <!--lsof command-not-found-->
                      <run>rm -rf /var/lib/apt/lists/*</run>
                    </runs>
                    <env>
                      <LD_LIBRARY_PATH>/opt/c2mon/${project.artifactId}/lib_so</LD_LIBRARY_PATH>
                    </env>
                    <volumes>
                      <volume>/opt/c2mon/${project.artifactId}/log</volume>
                      <volume>/opt/c2mon/${project.artifactId}/conf</volume>
                    </volumes>
                    <resources>
                       <resource>
                         <directory>${project.build.directory}/c2mon-${project.version}/c2mon-daq-${c2mon.daqname}/</directory>
                         <targetPath>/opt/c2mon/${project.artifactId}</targetPath>
                       </resource>
                       <resource>
                         <directory>${project.build.directory}/classes/conf</directory>
                         <targetPath>/opt/c2mon/${project.artifactId}/conf</targetPath>
                       </resource>
                       <resource>
                         <directory>${project.build.directory}/classes</directory>
                         <includes><include>docker-entrypoint.sh</include></includes>
                         <targetPath>/</targetPath>
                       </resource>
                    </resources>
                </configuration>
              </plugin>
        </plugins>
      </pluginManagement>

    </build>
    
    <profiles>
		<profile>
		  <id>rpi</id>
		  <properties>
		    <base.image>hypriot/rpi-java</base.image>
		    <arch.suffix>-arm</arch.suffix>
		  </properties>
		</profile>
    </profiles>

    <properties>
      <base.image>java:8-jdk</base.image>
      <c2mon.daqname>parent</c2mon.daqname>
      <arch.suffix></arch.suffix>
    </properties>

</project>
