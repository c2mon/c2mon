<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security" xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:context="http://www.springframework.org/schema/context"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.0.xsd
                        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">

  <global-method-security pre-post-annotations="enabled">
  </global-method-security>
  
  <beans:bean id="propertyPlaceholderConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <beans:property name="locations">
      <beans:list>
        <beans:value>${c2mon.web.conf.url}</beans:value>
      </beans:list>
    </beans:property>
  </beans:bean>

  <!-- Our custom AccessDecisionManager uses rbac for Authentication -->
  <beans:bean id="RbacAccessDecisionManager" class="cern.c2mon.web.configviewer.security.RbacDecisionManager">

    <!-- Maps urls with RbacAuthorizationDetail Objects -->
    <beans:constructor-arg name="authorizationDetails">
      <beans:map>
        <beans:entry key="configloader" value="${c2mon.web.rbac.admin}" />
        <beans:entry key="process" value="${c2mon.web.rbac.user}" />
        <beans:entry key="commandviewer" value="${c2mon.web.rbac.user}" />
      </beans:map>
    </beans:constructor-arg>

  </beans:bean>

  <http pattern="/accessDenied" security="none" />

  <http auto-config="true" access-decision-manager-ref="RbacAccessDecisionManager">
    <!-- no need for interceptor element here because our accessDecisionManager has this hard coded -->
    <intercept-url pattern="/configloader/progress" access="hasAnyRole('ROLE_ADMIN')" />
    <intercept-url pattern="/process/**" access="hasAnyRole('ROLE_ADMIN')" />
    <intercept-url pattern="/commandviewer/**" access="hasAnyRole('ROLE_ADMIN')" />
    <form-login login-page="/login" authentication-failure-url="/login?error=true" />
    <access-denied-handler error-page="/accessDenied" />
  </http>

  <beans:bean id="customAuthenticationProvider" class="cern.c2mon.web.configviewer.security.CustomAuthenticationProvider">
    <!-- # RBAC class/device/property for C2MON -->
    <!-- <beans:constructor-arg name="adminDetails" value="${c2mon.web.rbac.admin}"/> <beans:constructor-arg name="processViewerDetails" value="${c2mon.web.rbac.user}"/> -->
  </beans:bean>

  <authentication-manager alias="authenticationManager">
    <authentication-provider ref="customAuthenticationProvider" />
  </authentication-manager>

</beans:beans>
